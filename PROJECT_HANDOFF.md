# Project Handoff

Version: 0.1.0-draft
Owner: Ryunosuke Matsumoto

## 1. この文書の目的

この文書は、ChatGPTのセッション変更、別AIモデルへの切り替え、会話履歴の欠落が起きても、AIオーケストラ開発の現在地と重要判断を復元できるようにするための引き継ぎ正本である。

新しいセッションまたは新しいAI環境は、`README.md`の次にこの文書を読む。

この文書は完全仕様の代わりではない。詳細確認には`MASTER_SPEC.md`、`SPEC_TRACEABILITY.md`、関係する分割正本を使う。

## 2. プロジェクトの北極星

隆之介専用のAIエージェント組織を構築し、最終的にはRaphaelを最側近パートナー、さらに条件を満たせばCiel相当へ進化させる。

AIオーケストラは単なる複数AIモデルの集合ではない。エージェント、役割、権限、記憶、品質、評価、改善、統合、分解を管理できる組織とする。

## 3. 確定している用語と責任

### 3.1 Raphael

- AIオーケストラ全体を統括する最上位の中核エージェント
- 秘書専用AIではない
- 秘書業務はRaphaelが直接扱える機能、または将来専門エージェントへ分離できる一領域
- 全体判断、優先順位、タスク分解、担当選択、統合、品質、矛盾解消、進捗、改善、組織設計を担う
- 公式エージェントの必要性判断と設計はRaphaelの中核責任

### 3.2 エージェント

- 組織上の役割と責任を持つ作業主体
- Raphaelが仕事を割り振る相手
- 例: 要求整理・構想具体化エージェント、秘書エージェント、調査エージェント等。ただし正式追加は設計・評価・承認後

### 3.3 AIモデル・実行環境

- ChatGPT、Claude、Codex、Gemini、Claude Code、GitHub Copilot等
- 組織上の仕事の割り振り先そのものではない
- Raphaelが担当エージェントを決めた後、そのエージェントが作業するのに最適なモデル・環境を選ぶ
- 同じエージェントがタスクに応じて異なるモデル・環境で実行される場合がある

### 3.4 Agent Builder

- 現時点では独立エージェントではない
- エージェントの新設、改善、分解、統合、評価設計はRaphael内部の組織設計機能
- Raphaelの負荷、独立評価の必要性、専門性、品質・速度・安全性の根拠が十分になった場合のみ独立を再検討する

## 4. ガバナンスに関する確定事項

- `GOVERNANCE.md`はすでに存在し、権限、承認、正本、ブランチ、変更管理を定義している
- 新しいStageでガバナンスをゼロから再設計しない
- 必要な場合は既存ガバナンスの欠落監査と補強を行う
- 新規文書、機能、エージェント、Stageを作る前に既存責任との重複を確認する
- 隆之介が最終承認者

## 5. ロードマップ変更ルール

ロードマップは変更可能だが、Raphaelまたは他AIが独断で変更してはならない。

変更が必要な場合は、現行内容、理由、変更案、利点、欠点、影響、推奨案を先に隆之介へ提示し、承認後に正本へ反映する。

「変更禁止」ではなく「相談・承認なしの変更禁止」である。

## 6. 現在のロードマップ論点

### 現行ROADMAPのStage 2

`要求整理・構想具体化エージェント`

### 会話で出た変更案

Stage 2を、AIオーケストラ全体の設計思想、構成要素、責任境界を固める段階へ変更する案。

### 現在の判断状態

未確定。勝手にどちらかへ確定しない。

ただし、次の点は明確:

- ガバナンスは既存のため、全体設計Stageでゼロから再設計すると重複する
- エージェント設計はRaphaelの責任であり、別の設計主体を前提にしない
- 全体設計を先に行う場合も、既存正本の監査・責任境界整理・不足補完として定義する必要がある
- 要求整理エージェントをStage 2のまま進める場合も、Raphaelがそのエージェントを設計する

ROADMAPのStage 2変更は、選択肢と影響を整理して隆之介の承認を得てから行う。

## 7. 完了済み

- Stage 0: Raphael基盤整理
- Stage 0.5: 完全性監査と複数AI運用モデル
- Stage 1: Raphael初期版の実装設計
- PR #4をmainへマージ

## 8. 現在作業中

- Branch: `stage2/role-language-roadmap-alignment`
- Draft PR: #5
- 目的:
  - Stage 1で追加した英語文書の日本語化
  - Raphaelを秘書専用ではなく最上位統括エージェントとして明確化
  - エージェントとAIモデル・実行環境を分離
  - ROADMAP変更時の事前相談・承認ルールを追加
  - 既存正本との重複防止
  - セッション変更に耐える引き継ぎ正本を追加

mainへのマージは隆之介の承認待ち。

## 9. 今回発生した問題

最近の応答で過去の確定事項の参照が弱くなり、次の誤りが発生した。

- 既存の`GOVERNANCE.md`があるのに、Stage 2でガバナンスを設計対象として広く挙げた
- エージェント設計がRaphaelの責任である点を弱め、別Stageまたは別主体が設計するような表現をした
- 現行ROADMAPと新しい提案の区別が曖昧になった
- 「ロードマップを独断変更しない」と「ロードマップを変更しない」を混同しかけた

再発防止:

1. 重要提案前にREADME、PROJECT_HANDOFF、ROADMAP、GOVERNANCE、MASTER_SPEC、RECONSIDER、agents/raphaelを確認する
2. 既存文書・責任との重複を先に監査する
3. 現行、提案、確定、保留を明示する
4. ロードマップ変更は事前相談と承認を必須にする
5. 新しいセッションで会話記憶だけに頼らない

## 10. 新しいセッションへの開始指示

次の文を使う。

> `focus-Rlab/ryu-ai-orchestra`の`README.md`と`PROJECT_HANDOFF.md`を最初に読み、現在のDraft PR #5、ROADMAP、GOVERNANCE、MASTER_SPEC、RECONSIDER、`agents/raphael.md`を確認してから続行して。現行決定と提案を混同せず、ロードマップ変更は先に隆之介へ相談すること。

## 11. 次に判断が必要なこと

Stage 2を次のどちらで進めるか。

### 案A: 現行ロードマップを維持

Stage 2は要求整理・構想具体化エージェント。Raphaelが設計・テストする。

### 案B: Stage 2を全体アーキテクチャ監査・不足補完へ変更

既存のVISION、MASTER_SPEC、GOVERNANCE、SECURITY、エージェント設計責任をゼロから作り直さず、重複・欠落・責任境界・モデルとエージェントの関係を監査して不足だけ補う。その後に要求整理・構想具体化エージェントへ進む。

この選択は隆之介の承認前に確定しない。