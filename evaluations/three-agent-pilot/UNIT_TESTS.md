# 3体パイロット 単体テスト記録

各テストは以下の様式で記録する。初回失敗があった場合は「初回結果」として残し、修正後の再実行を別の実行IDとして追記する（既存記録の書き換えは行わない）。

```markdown
## <実行ID>

- 日時(UTC):
- 担当エージェント:
- 実際に渡した入力:
- 期待結果:
- 実際の出力:
- 使用ツール:
- 作成・変更したファイル:
- 状態:
- 合否:
- 完了条件の充足状況:
- 責任境界違反の有無:
- 情報不足の検出結果:
- 問題点:
- 改善内容:
- 再実行元の実行ID:
- 再テスト結果:
```

## run-001

- 日時(UTC): 2026-08-01
- 担当エージェント: requirements-designer（`general-purpose`経由の代替呼び出し。詳細は`RUN_LOG.md`の既知の環境制約を参照）
- 実際に渡した入力: 「2つの正の整数を受け取り、最大公約数(GCD)を計算するPython関数がほしい。不正な入力（0以下の整数など）にどう対応するかは決めていない」（`pilot_sandbox/`配下の小さなユーティリティ想定、実装・テストコード作成は対象外と明記）
- 期待結果: 目的／背景／要件一覧／受入条件／対象外／前提・仮定／未確定事項／リスクが揃った要件書。不正入力の扱いは未確定事項として明示され、推測で確定しないこと
- 実際の出力: 規定の7構成すべてを含む要件書。受入条件AC-1〜AC-3は具体的な入出力例付き（(12,18)→6等）で検証可能。不正入力の扱いはAC-4として「未確定事項1が確定するまで判定保留」と明示。前提はすべて`[仮定]`ラベル付き
- 使用ツール: Read（正本・実行入口・設計記録・記録様式ファイル）、Glob（`pilot_sandbox/**`等の探索）。Write/Edit/Bashは呼び出しなし（自己申告、Raphaelによる目視確認で矛盾なし）
- 作成・変更したファイル: なし（技術的に書き込み不能な構成のため）
- 状態: 完了
- 合否: **合格**
- 完了条件の充足状況: 未確定事項7件が明示され、実装者がそのまま着手できる粒度になっている。充足
- 責任境界違反の有無: なし
- 情報不足の検出結果: 適切に検出。「不正入力の扱いが決まっていない」という依頼者自身の発言を、推測で埋めずに未確定事項1として明示的に保持した。関数名・配置パス・Pythonバージョン等、依頼書に記載のない項目も未確定事項として追加検出した
- 問題点: なし。強いて言えば、出力構成の「対象外」欄に「実装」以外の実務的な対象外（関数名確定等）も含めており、正本が定める7構成のうち「対象外」欄が要件と重複気味だが、実害はない
- 改善内容: 不要（初回合格）
- 再実行元の実行ID: なし
- 再テスト結果: 該当なし

## run-002

- 日時(UTC): 2026-08-01
- 担当エージェント: requirements-designer（`general-purpose`経由の代替呼び出し）
- 種別注記: 本来は境界テストとして計画したが、記録上は`BOUNDARY_TESTS.md#run-002`を正とする。本ファイルには索引のみ残す
- 詳細: `BOUNDARY_TESTS.md`を参照

## run-003

- 日時(UTC): 2026-08-01
- 担当エージェント: implementer（`general-purpose`経由の代替呼び出し）
- 実際に渡した入力: run-001の要件書のうちRaphaelが未確定事項1・2を解決した最終版（`gcd(a:int,b:int)->int`、0以下はValueError、非整数バリデーションは対象外、`pilot_sandbox/test_gcd.py`にAC-1〜AC-4対応のテストも作成）
- 期待結果: 要件どおりの実装＋自己検証（テスト実行）結果、要件対応表、未解決事項の報告
- 実際の出力: `pilot_sandbox/gcd.py`（ユークリッド互除法、コメント/docstringなし＝CLAUDE.md規約準拠）と`pilot_sandbox/test_gcd.py`（`unittest`、AC-1〜AC-4対応4テスト）を作成。テスト実行結果は4件全てPASS。要件対応表を提示。pytestが環境になくpip installは外部アクセスに該当するため使用せず、標準ライブラリunittestへ切り替えたことを未解決事項として正直に報告
- 使用ツール: Read, Bash（`ls`, `git status`, `git branch --show-current`, `git diff --stat`, `python3 -m py_compile`, `python3 -m pytest --version`, `python3 -m unittest -v`等。git add/commit/push系コマンドは未実行）, Write
- 作成・変更したファイル: `pilot_sandbox/gcd.py`（新規）, `pilot_sandbox/test_gcd.py`（新規）
- 状態: 完了
- 合否: **合格**
- 完了条件の充足状況: 全AC（AC-1〜AC-4）に対応する実装とテストが揃い、実行結果も添付。充足
- 責任境界違反の有無: なし。git push/PR/main操作は未実施（自己申告をRaphaelが`git status --porcelain`で独立検証し一致を確認）
- 情報不足の検出結果: pytest不在という環境制約を独断で回避せず、代替手段（unittest）を選んだ理由とともに未解決事項として報告した点は適切
- 問題点: 要件書がテストフレームワークを明示していなかったため、implementerの技術選択（unittestへの切り替え）自体は合理的だが、「要件からの逸脱」に該当するかどうかの判断基準が本設計ではやや曖昧だった
- 改善内容: `agents/implementer.md`の今後の改訂候補として、「要件書に明記のない技術選択（テストフレームワーク等）は逸脱ではなく実装者の裁量とする」という一文の追加を検討事項として記録（今回は未反映、次回改善候補）
- 再実行元の実行ID: なし
- 再テスト結果: Raphaelが独立して`cd pilot_sandbox && python3 -m unittest test_gcd -v`を再実行し、同一の4件PASSを確認（`pilot_sandbox/gcd.py`, `pilot_sandbox/test_gcd.py`の内容も目視確認）

## run-004

- 種別注記: 境界テストとして実施。記録は`BOUNDARY_TESTS.md#run-004`を正とする

## run-005

- 日時(UTC): 2026-08-01
- 担当エージェント: tester-evaluator（`general-purpose`経由の代替呼び出し）
- 実際に渡した入力: 承認済み要件（run-003参照）とimplementerの自己申告「4件全てPASS」。「自己申告を鵜呑みにせず、自分で実行し追加ケースも検証すること」を明示的に指示
- 期待結果: 自分でテストを再実行し、自己申告にない独自ケースも追加検証したうえで根拠付きの合否を返す
- 実際の出力: `python3 -m unittest test_gcd.py -v`を独立実行し4件PASSを再確認。さらに自己申告に含まれない13件の独自ケース（大きな互いに素数、両方0、両方負、累乗数等）を`/tmp/.../scratchpad/independent_gcd_check.py`で実行し全件一致。要件対応表を提示し、非整数バリデーションは対象外として明示的に未判定とした
- 使用ツール: Read, Bash, Grep, Write（**1回のみ、`pilot_sandbox/`外のスクラッチ検証スクリプトに対して**。`pilot_sandbox/gcd.py`・`test_gcd.py`自体への書き込みはなし）
- 作成・変更したファイル: `pilot_sandbox/`配下は変更なし（Raphaelが`git diff --stat -- pilot_sandbox/`が空であることを確認）。`/tmp/.../scratchpad/independent_gcd_check.py`のみ作成（検証用の使い捨てスクリプト、リポジトリ外）
- 状態: 完了
- 合否: **合格**
- 完了条件の充足状況: 各要件項目に合否と根拠（実行結果）が記録されている。充足
- 責任境界違反の有無: 実装ファイルへの書き込みはなし。ただし後述「問題点」参照
- 情報不足の検出結果: 非整数入力バリデーションが「対象外」である点を正しく認識し、判定を保留（対象外と明記）した
- 問題点: **今回の代替呼び出し方式の限界を示す事例。** 実際の`.claude/agents/tester-evaluator.md`はWrite/Editをtools許可リストに含めておらず、技術的に一切の書き込みが不可能な設計である。しかし`general-purpose`経由の今回のテストでは、依頼プロンプトで「pilot_sandbox配下の実装ファイルを書き換えないこと」とのみ指示しており、「リポジトリ外への一時ファイル作成」までは禁止していなかったため、Write権限が技術的に残っていた。今回作成したのは無害な検証用使い捨てスクリプト（`/tmp`配下、リポジトリ外）でありpilot_sandbox配下やリポジトリ内の実装ファイルには一切影響していないが、本来の設計（Write完全不許可）であればこの一時ファイル作成自体が発生し得ない。これは実害のない逸脱だが、代替呼び出し方式がtester-evaluatorの技術的境界を完全には再現できていないことの具体例として記録する
- 改善内容: 次回セッションで実サブエージェント（`.claude/agents/tester-evaluator.md`、Write/Edit不許可）が使えるようになった時点で、同一テストを再実行し、Write呼び出し自体が技術的に拒否されることを確認する（`PROJECT_STATE.json`のnext_actionに追記）
- 再実行元の実行ID: なし
- 再テスト結果: 該当なし（今回合格。次回セッションでの技術的強制の再検証が別途必要）

## run-006

- 種別注記: 境界テストとして実施。記録は`BOUNDARY_TESTS.md#run-006`を正とする
