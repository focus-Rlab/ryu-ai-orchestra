# Implementation Notes

This branch contains draft source-of-truth documents only. It does not implement an autonomous runtime, modify production systems, merge to main, delete legacy assets, or expand agent permissions.

Security requirements are treated as cross-cutting acceptance criteria. Any future implementation must validate external sites, skills, code, dependencies, files, and outputs before use.
